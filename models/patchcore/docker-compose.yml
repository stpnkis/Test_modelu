# =============================================================================
# Docker Compose - PatchCore Experiment
# =============================================================================
# Run from this directory: docker compose run --rm patchcore python src/run_benchmark.py
# GPU access via NVIDIA Container Toolkit (requires: docker context use default)
# =============================================================================

services:
  patchcore:
    build: .
    shm_size: '8gb'
    environment:
      - PYTHONPATH=/app
    volumes:
      # Datasets (read-only)
      - ../../datasets:/app/datasets:ro
      # Git metadata for commit traceability in results
      - ../../.git:/app/.git:ro
      # Centralized splits (shared across models)
      - ../../splits:/app/splits
      # Centralized experiment results (shared across models)
      - ../../experiments:/app/experiments
      # Shared benchmark module
      - ../../shared:/app/shared:ro
      # Live source code mount — edit without rebuilding the image
      - ./src:/app/src
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    stdin_open: true
    tty: true
