# =============================================================================
# Docker Compose - RD++ Experiment
# =============================================================================
# Run from this directory: docker compose run --rm rd_plus_plus python src/experiment_runner.py
# GPU access via NVIDIA Container Toolkit (requires: docker context use default)
# =============================================================================

services:
  rd_plus_plus:
    build: .
    shm_size: '8gb'
    volumes:
      # Shared dataset from repository root (read-only)
      - ../../dataset:/app/dataset:ro
      # Experiment results persist in repository root (per-model subfolder)
      - ../../experiments/rd_plus_plus:/app/experiments
      # Dataset splits (generated during experiments)
      - ../../splits/rd_plus_plus:/app/splits
      # Live source code mount — edit without rebuilding the image
      - ./src:/app/src
      # Tests mount — edit without rebuilding the image
      - ./tests:/app/tests
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    stdin_open: true
    tty: true
