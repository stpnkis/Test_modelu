# Host-side dependencies for the benchmark CLI and test suite.
# Models run inside Docker containers with their own requirements.
#
# Install:  pip install -r requirements.txt

torch>=2.0
torchvision>=0.15
numpy>=1.24
scikit-learn>=1.2
scipy>=1.10
PyYAML>=6.0
Pillow>=9.0
pytest>=7.0
